- if attr? 'canvas-image'
  %section{:id=>@id, :class=>['slide', 'canvas-image', role], :style=>"background-image: url(#{attr 'canvas-image'})"}
    =content.chomp
- else
  %section.slide{:id=>@id, :class=>role}
    - if has_role? 'title'
      %h1=title
    - else
      - header=%(<h#{@level+1}>#{title}</h#{@level+1}>)
      =header
    =content.chomp
